// =============================================================================
// MirageSystem v2 GUI - Global State Implementation
// =============================================================================
// Note: All state is now managed via MirageContext singleton (mirage_context.hpp)
// This file is kept for CMake compatibility but contains no definitions.
// =============================================================================
#include "gui_state.hpp"

// All variables and functions are now defined in:
// - mirage_context.hpp (inline references via ctx())
// - mirage_context.cpp (singleton implementation)
